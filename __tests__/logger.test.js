// __tests__/logger.test.js

const logger = require('../utils/logger');

describe('Logger', () => {
  it('should be defined', () => {
    expect(logger).toBeDefined();
  });

  // More tests can be added here, such as checking that log messages are formatted as expected.
});
